<UserControl x:Class="LookupFireDamageTest.ParcelDockpaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
						 Width="300" Height="400"
             xmlns:local="clr-namespace:LookupFireDamageTest">
  <StackPanel Margin="10">
    <Button Content="Select Parcel" Command="{Binding SelectParcelCommand}" Margin="0,0,0,10" />
    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
      <TextBlock Text="APN:" VerticalAlignment="Center" Width="50" />
      <TextBox Text="{Binding APN}" IsReadOnly="True" Width="200" />
    </StackPanel>
    <StackPanel Orientation="Horizontal">
      <TextBlock Text="Address:" VerticalAlignment="Center" Width="50" />
      <TextBox Text="{Binding Address}" Width="200" />
    </StackPanel>

    <StackPanel Margin="0,40,0,0">
      <TextBlock Text="Damage Assessment Records" FontWeight="Bold" Margin="0,0,0,5" />
      <ComboBox x:Name="DamageAssessmentRecords" ItemsSource="{Binding DamageAssessmentRecords}" SelectedItem="{Binding SelectedDamageAssessmentRecord}" Width="200" Margin="0,0,0,10" />
      <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
        <TextBlock Text="Damage Level:" VerticalAlignment="Center" Width="90" />
        <TextBox Text="{Binding Damage}" IsReadOnly="True" Width="110" />
      </StackPanel>
      <Image Source="{Binding DamageImage}" Stretch="Uniform" Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=UserControl}}" Height="100" />
    </StackPanel>
  </StackPanel>
</UserControl>
