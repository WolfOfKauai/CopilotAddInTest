<!-- file: E:\Repos\WolfOfKauai\CopilotAddInTest\MyCopilotAddin\LotCreationDockPaneView.xaml -->
<UserControl x:Class="MyCopilotAddin.LotCreationDockPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:MyCopilotAddin"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="300">
  <UserControl.Resources>
    <local:StringNotEmptyConverter x:Key="StringNotEmptyConverter"/>
  </UserControl.Resources>
  <StackPanel Margin="10">
    <TextBlock Text="Selected Parcel ID:" Margin="0,0,0,5"/>
    <TextBox Text="{Binding ParcelId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" Margin="0,0,0,10"/>
    <TextBlock Text="Multi-patch Resolution:" Margin="0,0,0,5"/>
    <ComboBox ItemsSource="{Binding Resolutions}" SelectedItem="{Binding SelectedResolution, Mode=TwoWay}" Margin="0,0,0,10"/>
    <Button Content="Create 3D Lots" Command="{Binding Create3DLotCommand}" IsEnabled="{Binding ParcelId, Converter={StaticResource StringNotEmptyConverter}}" />
  </StackPanel>
</UserControl>