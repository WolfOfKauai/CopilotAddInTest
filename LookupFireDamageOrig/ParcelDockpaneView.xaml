<UserControl x:Class="LookupFireDamageOrig.ParcelDockpaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="300">
    <StackPanel Margin="10">
        <Button Content="Select Parcel" Width="120" Height="30" Margin="0,0,0,10" Command="{Binding SelectParcelCommand}" />
        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
            <Label Content="APN:" Width="50" VerticalAlignment="Center" />
            <TextBox Text="{Binding APN}" IsReadOnly="True" Width="200" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Address:" Width="50" VerticalAlignment="Center" />
            <TextBox Text="{Binding Address}" Width="200" />
        </StackPanel>
        <Label Content="Damage Assessment Records" Margin="0,10,0,2" />
        <ComboBox x:Name="DamageAssessmentRecords" ItemsSource="{Binding DamageAssessmentRecords}" SelectedItem="{Binding SelectedDamageAssessmentRecord}" DisplayMemberPath="RecordName" Width="Auto" />
        <Label Content="Damage Level" Margin="0,10,0,2" />
        <TextBox Text="{Binding DamageLevel}" IsReadOnly="True" Width="Auto" />
        <Image Source="{Binding DamageImage}" Stretch="Uniform" Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="0,10,0,0" />
    </StackPanel>
</UserControl>
